PORT=3000

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tanh79c84@gmail.com
SMTP_PASS=ijgx kipb vivc ttor
MAIL_FROM="MeoMeoFindMovie <tanh79c84@gmail.com>"

# Để "*" khi chạy local. Khi deploy thật, đổi thành domain frontend, vd:
# ALLOWED_ORIGIN=https://ten-app.onrender.com
ALLOWED_ORIGIN=*

# Khóa bí mật dùng để hash mã OTP và ký verifyToken (Lớp 2 & Lớp 4 trong
# README). Nên đặt 1 chuỗi ngẫu nhiên dài, giữ kín, KHÔNG commit lên git.
# Có thể tự sinh bằng lệnh: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Nếu để trống, server tự sinh khóa tạm mỗi lần khởi động (vẫn an toàn,
# chỉ mất hiệu lực các verifyToken đang treo nếu server restart giữa chừng).
OTP_SECRET=doi-thanh-chuoi-ngau-nhien-dai-va-giu-kin
